<div class="body">
  <div class="container mt-5">
    <div class="row">
      <div class="mx-auto">
        <h2><%= @team.teamname %></h2>
        <%= month_calendar events: @team do |date, teams| %>
          <%= date.day %>
        
          <% teams.each do |team| %>
            <div>
             
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>
